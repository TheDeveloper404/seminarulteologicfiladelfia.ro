import type { GalleryAlbum } from "./types";

// Populat via `scripts/upload-gallery.ts` — vezi manifestul TS printat de script
// și lipește-l aici după fiecare upload de album în Vercel Blob.
export const galerii: GalleryAlbum[] = [];
